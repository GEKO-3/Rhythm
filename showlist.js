// Show List Configuration
// Add song names in the exact order you want them to appear when "SHOW" filter is selected
// If this array is empty, the "SHOW" filter option will not appear in the dropdown
// Song names must match exactly with the names in songs.js

const showList = [

];

// Export for use in other files
if (typeof module !== 'undefined' && module.exports) {
  module.exports = showList;
}
